{
  "name": "eyes4beat",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "deploy": "npm run build && wrangler pages deploy",
    "preview": "vite preview",
    "test": "node --test"
  },
  "devDependencies": {
    "vite": "^7.1.0",
    "wrangler": "^4.135.0"
  },
  "dependencies": {
    "lucide": "^1.47.0"
  }
}
